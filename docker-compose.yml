version: '3.8'

services:
  web:
    build: .
    ports:
      - "0.0.0.0:5000:5000"
    environment:
      - MINIO_ENDPOINT=106.12.107.176:19000
      - MINIO_ACCESS_KEY=minio
      - MINIO_SECRET_KEY=ei2BEHZYLaR8eGtT
      - MINIO_BUCKET=album
      - MINIO_SECURE=false
      - FLASK_SECRET_KEY=your-secret-key-here
      - ACCESS_PASSWORD=minioshare123
      - FLASK_DEBUG=false
    restart: unless-stopped 